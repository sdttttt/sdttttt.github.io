# CLAUDE.md

Claude Code 入口提示：本仓库的完整规范已迁移到 [`AGENTS.md`](AGENTS.md)，所有维护流程、命名约定、CI 行为、Agent 维护日志模板都在那里。

本文件仅保留 Claude Code 特有的提示：会话内优先使用 `rg`（ripgrep）而非 `grep` / `find`；commit / push / PR 等对外动作前必须先与用户确认。
