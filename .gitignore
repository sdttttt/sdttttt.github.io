# Dependencies
node_modules/

# Hugo build
public/
.hugo_build.lock
hugo_stats.json

# OS
.DS_Store

# Local Deno toolchain (binary + module cache)
.tools/
.npm-cache/

# Scripts: Deno runs .ts directly, scripts/dist/ is a dead artifact
# from an old Node.js-era snapshot. Never commit it.
scripts/dist/
