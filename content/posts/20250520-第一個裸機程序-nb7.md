---
title: "第一個裸機程序"
date: 2025-05-20
description: "使用 bochs 虛擬機編寫並運行第一個裸機程序的記錄,介紹 bochs 配置、彙編啟動代碼編寫以及最簡運行環境搭建的完整流程。"
author: sdttttt
draft: false
cover:
  image: "images/covers/20250520-第一個裸機程序-nb7.svg"
  alt: ""
  hidden: false
aliases: ["/posts/2025052001n2kq/"]
language: "zh-tw"
---

今天嘗試寫我的第一個裸機程序，虛擬機使用bochs，這個虛擬機配置很容易，也比較簡單，非常新手。

```c
# bochs 配置文件

# 設置Bochs 在運行過程中能夠使用的內存，32MB
megs: 32

# BIOS和顯示BIOS
romimage: file=/home/admin123/Desktop/bochs/share/bochs/BIOS-bochs-latest
vgaromimage: file=/home/admin123/Desktop/bochs/share/bochs/VGABIOS-lgpl-latest

# 軟盤，不用
# floppya: 1_44=a.img, status=inserted

# 使用硬盤
# boot: floppy
boot: disk

# 日誌文件的輸出。
log: bochs.out

# 關閉鼠標
mouse: enabled=0
keyboard_mapping: enabled=1,map=/home/admin123/Desktop/bochs/share/bochs/keymaps/x11-pc-us.map

# 硬盤設置
ata0-master: type=disk, path="hd60M.img", mode=flat, cylinders=121, heads=16, spt=63
ata0: enabled=1, ioaddr1=0x1f0, ioaddr2=0x3f0, irq=14

# gdb的支持，1234 端口調試
gdbstub: enabled=0, port=1234, text_base=0, data_base=0, bss_base=0

```

```c
[org 0x7c00]
[bits 16]
BOOT_MAIN_ADDR equ 0x500
readdiskmsg db "ReadDisk...", 0

global _start

_start:
    xor ax, ax
    mov ds, ax
    mov es, ax
    mov ss, ax
    mov fs, ax
    mov sp, 0x7c00

    mov ax, 3
    int 0x10

    mov ecx, 2
    mov bl, 2

    mov dx, 0x1f2
    mov al, bl
    out dx, al

    inc dx
    mov al, cl
    out dx, al

    inc dx
    mov al, ch
    out dx, al

    inc dx
    shr ecx, 16
    mov al, cl
    out dx, al

    inc dx
    mov al, ch
    and al, 0b1110_1111
    out dx, al

    inc dx
    mov al, 0x20
    out dx, al

    mov si, readdiskmsg
    call print

.hd_ready_check:
    mov dx, 0x1f7
    in al, dx
    and al, 0b0000_1000
    cmp al, 0b0000_1000
    jnz .hd_ready_check

    mov dx, 0x1f0
    mov cx, 256
    mov edi, BOOT_MAIN_ADDR

.read_boot:
    in ax, dx
    mov [edi], ax
    add edi, 2
    loop .read_boot

    jmp BOOT_MAIN_ADDR

;; mov si, string
;; call print
print:
    mov ah, 0x0e
    mov bh, 0
    mov bl, 0x01
.print_loop:
    mov al, [si]
    cmp al, 0
    jz .print_done
    int 0x10

    inc si
    jmp .print_loop

.print_done:
    ret

times 510 - ($ - $$) db 0
db 0x55, 0xaa
```

彙編具體就不解釋了，太累了。

我沒有在這個代碼裡分段，因為分段之後編譯出來的二進制會有內存對齊，導致最後大小不是512字節。

運行效果：

![image.png](image%202.png)
