---
title: "尝试写一个strlen"
date: 2025-04-25
description: "展示使用 NASM 编写字符串长度函数的过程，通过逐字节扫描并在结束时计算地址差值返回字符串长度。"
author: sdttttt
draft: false
cover:
  image: "images/covers/20250425-尝试写一个strlen-xm6.svg"
  alt: ""
  hidden: false
aliases: ["/posts/2025042506n1r6/"]
---

```c
#include <stdio.h>

extern int strlen2(char *str);

int main() {
        char a[] = "hello,world";
        int len = strlen2(a);

        printf("%s len:%d  \n", a, len);

        return 0;
}
```

```nasm
SECTION .data

SECTION .text
	global strlen2

strlen2:
	mov rax, rdi
next:
	cmp byte [rax], 0
	je down
	inc rax
	jmp next
down:
	sub rax, rdi
	ret
```
