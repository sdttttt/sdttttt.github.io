---
title: "SmartDNS 配置"
date: 2023-09-21
description: "說明 SmartDNS 的測速、域名解析、隱私保護和廣告過濾能力，並提供日誌、緩存、上游分組及 DoT/DoH 配置示例。"
author: sdttttt
draft: false
cover:
  image: "images/covers/20230921-SmartDNS-配置-6o4.svg"
  alt: ""
  hidden: false
aliases: ["/posts/2023092100r5kh/"]
language: "zh-tw"
---

- SmartDNS 是一個運行在本地的 DNS 服務器，它接受來自本地客戶端的 DNS 查詢請求，然後從多個上游 DNS 服務器獲取 DNS 查詢結果，並將訪問速度最快的結果返回給客戶端，以此提高網絡訪問速度。 SmartDNS 同時支持指定特定域名 IP 地址，並高性匹配，可達到過濾廣告的效果; 支持DOT(DNS over TLS)和DOH(DNS over HTTPS)，更好的保護隱私。

這個是官方上寫的一段話, 我打算使用SmartDNS的原因, 第一是因為Clash的DNS解析多多少少有點慢, 第二是這個DNS的配置看起來還算比較容易上手.

唯一感到有美中不足的是沒有統計接口, 看不到各個DNS的一個數據情況.

```
# Add custom settings here.

# set log level
# log-level [level], level=fatal, error, warn, notice, info, debug
log-level info # 日誌等級

# log-size k,m,g
log-size 1m  # 日誌大小

# log-file /var/log/smartdns.log
# log-num 2

# List of hosts that supply bogus NX domain results
# bogus-nxdomain [ip/subnet] # 被汙染的IP

cache-size 512 # 緩存大小, 條目

speed-check-mode tcp:80,tcp:443,ping # 測速手段, 最優先的是80端口的TCP測速

# -group domestic組的DNS
# 同時也是默認使用的DNS
server 192.168.1.1 -group domestic
server 202.101.172.35 -group domestic
server-tls 1.12.12.12:853 -group domestic
server-tls 120.53.53.53:853 -group domestic
server-tls 223.5.5.5:853 -group domestic
server-tls 223.6.6.6:853 -group domestic
server 114.114.114.114 -group domestic
server 114.114.115.115 -group domestic
server 1.2.4.8 -group domestic
server 210.2.4.8 -group domestic

# -group fq_dns 第二組DNS
# ----- Default Group -----
# 默認使用的上游 DNS 組
# OpenDNS 非常規 443 端口、支持 TCP 查詢
server-tcp -group fq_dns 208.67.220.220:443
# OpenDNS 的 IP DoH
server-https https://146.112.41.2/dns-query -group fq_dns
# TWNIC 的 IP DoH
server-https https://101.101.101.101/dns-query -group fq_dns
# 你也可以配置其它 DNS 作為上游
server-tls 1.1.1.1:853 -group fq_dns
server-https https://jp.tiar.app/dns-query -group fq_dns
server-https https://jp.tiarap.org/dns-query -group fq_dns
```
