---
title: "2026-07-22"
date: 2026-07-22T00:00:00+08:00
tags: ["维护记录"]
---

## 维护记录

### 完成的工作

- 将博客默认主题从 `auto` 切换为 `dark`，访问者首次进入站点时将直接以 Dark 模式浏览 ✅
- 将最近两篇文章（《意义》《关于恋爱观》）标记为 `private: true`，不在文章列表/首页展示，但保留直接访问链接 ✅
- 新增 `.github/workflows/gen-covers.yml`，在 `content/posts/**.md` 变更时自动运行 `bun scripts/gen-covers.ts --all --inject-fm` 生成封面并注入 front matter ✅
- 提取 `scripts/lib/args.ts` 和 `scripts/lib/git.ts` 共享工具，新增 `scripts/git-commit-push.ts` ✅
- 改造 `format-markdown.yml` 和 `gen-covers.yml`，统一使用 `bun scripts/git-commit-push.ts` 提交变更 ✅
- 新增 `scripts/format-markdown.ts`，`format-markdown.yml` 的格式化步骤改为 `bun` 执行 ✅

### 遇到的问题

- 无 ⚠️

### 下次建议

- 无
