---
title: "2026-07-22"
date: 2026-07-22T00:00:00+08:00
tags: ["维护记录"]
---

## 维护记录

### 完成的工作

- 将博客默认主题从 `auto` 切换为 `dark`，访问者首次进入站点时将直接以 Dark 模式浏览 ✅
- 将最近两篇文章（《意义》《关于恋爱观》）从 `private: true` 改为 Hugo 原生 `build.list: never + render: true`，在 /posts/ 列表中隐藏但保留直接访问链接 ✅
- 新增 `.github/workflows/gen-covers.yml`，在 `content/posts/**.md` 变更时自动运行 `bun scripts/gen-covers.ts --all --inject-fm` 生成封面并注入 front matter ✅
- 提取 `scripts/lib/args.ts` 和 `scripts/lib/git.ts` 共享工具，新增 `scripts/git-commit-push.ts` ✅
- 改造 `format-markdown.yml` 和 `gen-covers.yml`，统一使用 `bun scripts/git-commit-push.ts` 提交变更 ✅
- 新增 `scripts/format-markdown.ts`，`format-markdown.yml` 的格式化步骤改为 `bun` 执行 ✅
- 新增 `scripts/validate-posts.ts`、`scripts/sync-covers.ts`、`scripts/check-dead-links.ts` 三个脚本 ✅
- 抽离 `scripts/lib/frontmatter.ts`，`gen-covers.ts` 与校验脚本共享 front matter 解析 ✅
- 新增 `validate-posts.yml`、`sync-covers.yml`、`check-dead-links.yml` 三个 workflow ✅
- 精简 `layouts/partials/custom_footer.html`，只保留 Cloudflare Web Analytics 脚本 ✅

### 遇到的问题

- `validate-posts.ts` 初版未按 Hugo 资源规则查找 `assets/` 目录，导致大量封面误报不存在，已修复为同时查找 `assets/` 和 `static/` ⚠️
- `content/posts/2020-05-09-gfs.md` 使用 TOML front matter（`+++`），共享解析器只支持 YAML，已将其转换为 YAML 格式 ⚠️

### 下次建议

- 无
