---
title: "2026-07-24"
date: 2026-07-24
tags: ["維護記錄"]
language: "zh-tw"
---

## 維護記錄

### 完成的工作

- 修改 `.github/workflows/check-dead-links.yml` 中的 `Check dead links` 步驟，使用 `|| true` 讓該步驟始終成功，避免死鏈檢查失敗阻塞 CI ✅
- 修改 `scripts/check-dead-links.ts`，跳過位於 ` ``` ` 與 `~~~` 圍欄代碼塊內的鏈接，避免代碼示例中的 URL 被誤檢 ✅
- 為 `scripts/` 下所有非環境依賴腳本補充單元測試，共 106 個測試用例全部通過；同時修復了 `frontmatter.ts` 嵌套對象解析和 `gen-covers.ts` 的 `pick` 函數在處理負數時的行為 ✅

### 遇到的問題

- 無 ⚠️

### 下次建議

- 後續可考慮將死鏈檢查失敗結果以非阻塞方式上報（如 issue 或註釋），而非直接忽略
