---
title: "2026-08-29"
date: 2026-08-29
tags: ["维护记录"]
---

## 维护记录

### 完成的工作

- `.gitmodules` 移除孤儿条目 `[submodule "themes/paper"]`（指向已不存在的 `themes/paper`，源自 2026-03-17 的 `6d6051b Switch theme to PaperMod` 迁移）；本地 `.git/config` 不含此条目，gitlink 也已不存在，删除安全
- 新增 `.prettierignore` 屏蔽 `themes/PaperMod`（含 `node_modules` / `.tools` / `.npm-cache` / `public` 作为防御）；修掉 `format-markdown-check` 永远报 PaperMod 内 `PULL_REQUEST_TEMPLATE.md` / `README.md` 的误报
- 精简 `CLAUDE.md`：从 134 行压缩为 6 行指针，统一指向 `AGENTS.md`，仅保留 Claude Code 特有提示（优先用 `rg`、对外动作需确认）
- 清理本地残留 `node_modules/`（49M）与 `.npm-cache/`（3.4M）—— 均为 gitignored，不影响提交

### 遇到的问题

- 本机 `deno` 不在 PATH，但 `.tools/deno/bin/deno` 有 2.9.5 二进制 ⚠️ - 临时 `export PATH=$PWD/.tools/deno/bin:$PATH` 后跑 `validate-posts` / `test` / `sync-covers-dry` / `format-markdown-check`，CI 走 `denoland/setup-deno@v2` 无此问题

### 下次建议

- AGENTS.md 增加一段「deno 本地安装提示」：如果 `deno` 命令未找到，仓库根目录 `.tools/deno/bin/deno` 是可用回退（`export PATH=$PWD/.tools/deno/bin:$PATH`）
